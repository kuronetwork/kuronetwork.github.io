# 🚀 快速上線指南

## ✅ 網站已建置完成！

你的新網站已經準備好了！所有 59 篇文章都已經轉移完成。

### 📊 網站統計
- ✅ 59 篇文章已轉移
- ✅ 5 個分類（資訊安全、CTF/滲透測試、網路管理、讀書心得、理財投資）
- ✅ 響應式設計（手機、平板、桌面）
- ✅ 搜尋功能
- ✅ 分類篩選
- ✅ 現代化設計

### 🌐 本地測試

網站目前正在 http://localhost:3000 運行

訪問以下頁面測試：
- 首頁: http://localhost:3000
- 文章索引: http://localhost:3000/articles
- 關於: http://localhost:3000/about
- 聯絡: http://localhost:3000/contact

### 📝 如何新增文章

編輯 `data/articles.ts` 檔案，在陣列最前面加入：

```typescript
{
  id: 60,  // 遞增編號
  title: "新文章標題",
  date: "2024-12-14",
  category: "資訊安全",  // 或其他分類
  tags: ["標籤1", "標籤2"],
  url: "https://medium.com/blacksecurity/新文章連結",
  featured: false  // 設為 true 會在首頁顯示
},
```

### 🚀 部署到 GitHub Pages

#### 方法 1: 自動部署（推薦）

1. **建立新的 GitHub Repository**
   ```bash
   # 在 new-site 資料夾中
   git init
   git add .
   git commit -m "Initial commit: New portfolio website"
   ```

2. **推送到 GitHub**
   ```bash
   git branch -M main
   git remote add origin https://github.com/你的用戶名/kuronetwork.github.io.git
   git push -u origin main
   ```

3. **啟用 GitHub Pages**
   - 前往 GitHub repo 設定
   - 點擊 "Pages"
   - Source 選擇 "GitHub Actions"
   - 儲存

4. **等待部署完成**
   - 查看 Actions 頁面
   - 等待 2-3 分鐘
   - 訪問 https://kuronetwork.me

#### 方法 2: 手動部署

```bash
# 建置網站
npm run build

# 推送 out 資料夾
cd out
git init
git add .
git commit -m "Deploy"
git push -f https://github.com/你的用戶名/kuronetwork.github.io.git main:gh-pages
```

### 🔧 常用指令

```bash
# 開發模式
npm run dev

# 建置生產版本
npm run build

# 安裝依賴
npm install
```

### 📂 專案結構

```
new-site/
├── app/              # 頁面
│   ├── page.tsx      # 首頁
│   ├── articles/     # 文章索引
│   ├── about/        # 關於
│   └── contact/      # 聯絡
├── components/       # 元件
├── data/            # 文章資料（你要編輯的）
│   └── articles.ts  # 所有文章列表
├── public/          # 靜態資源
└── package.json
```

### ✨ 功能特色

- 🎨 現代化設計（青色主題，專業資安風格）
- 📱 完美響應式（手機、平板、桌面）
- 🔍 即時搜尋功能
- 🏷️ 分類篩選
- ⚡ 極快載入速度（靜態生成）
- 🔗 所有文章連結到 Medium
- 🌐 SEO 優化

### 🎯 下一步

1. 測試所有頁面是否正常
2. 確認文章連結都正確
3. 推送到 GitHub
4. 啟用 GitHub Pages
5. 等待部署完成
6. 訪問你的新網站！

### 💡 提示

- 文章資料在 `data/articles.ts`
- 個人資訊在 `app/about/page.tsx`
- 社群連結在 `app/page.tsx` 和 `app/contact/page.tsx`
- 配色在 `tailwind.config.ts`

---

**網站已經準備好上線了！** 🎉
